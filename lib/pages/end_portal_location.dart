import 'package:flutter/material.dart';

class EndPortalLocation extends StatefulWidget {
  EndPortalLocation({Key key}) : super(key: key);

  @override
  _EndPortalLocationState createState() => _EndPortalLocationState();
}

class _EndPortalLocationState extends State<EndPortalLocation> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}